    <span class="inline-flex size-6 relative">
        <span class="outer" />
        <span class="inner" />
    </span>

<style lang="postcss">
    .outer {
        @apply animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75;
    }
    .inner {
        @apply relative inline-flex rounded-full h-full w-full bg-green-500;
    }
</style>
